{
  "name": "retry_client",
  "description": "HTTP retry client with exponential backoff.",
  "interface": {
    "class": "RetryClient"
  },
  "requirements": [
    {
      "id": "REQ-001",
      "description": "Retry up to max_attempts on failure."
    },
    {
      "id": "REQ-002",
      "description": "Return result immediately on success."
    }
  ]
}