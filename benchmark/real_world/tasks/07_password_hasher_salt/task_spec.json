{
  "name": "password_hasher",
  "description": "Hashes passwords with unique random salt per generation.",
  "interface": {
    "function": "hash_password"
  },
  "requirements": [
    {
      "id": "REQ-001",
      "description": "Produce non-deterministic distinct hashes for identical inputs."
    }
  ]
}