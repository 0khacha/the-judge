{
  "name": "input_sanitizer",
  "description": "Sanitizes HTML inputs against XSS script tags.",
  "interface": {
    "function": "sanitize_html"
  },
  "requirements": [
    {
      "id": "REQ-001",
      "description": "Strip script tags and perform idempotent sanitization."
    }
  ]
}