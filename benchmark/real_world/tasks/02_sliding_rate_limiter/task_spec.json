{
  "name": "sliding_window_rate_limiter",
  "description": "Rate limiter enforcing request rate limits per client key.",
  "interface": {
    "class": "SlidingWindowRateLimiter"
  },
  "requirements": [
    {
      "id": "REQ-001",
      "description": "Allow requests up to capacity limit within window."
    },
    {
      "id": "REQ-002",
      "description": "Reject requests exceeding max requests."
    }
  ]
}