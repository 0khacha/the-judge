{
  "task_id": "16_sql_query_sanitizer",
  "name": "sql_query_sanitizer",
  "description": "SQL query parameterizer binding values into parameterized query placeholders safely.",
  "interface": {
    "class": "SQLQuerySanitizer"
  },
  "requirements": [
    {
      "id": "REQ-001",
      "description": "Convert raw SQL statement with inline parameters into parameterized SQL and parameter tuple.",
      "category": "security",
      "priority": "critical",
      "properties": [
        "parameterize_query"
      ],
      "examples": [
        "SELECT * FROM u WHERE name='alice' -> SELECT * FROM u WHERE name=?, ('alice',)"
      ],
      "edge_cases": [
        "SQL injection quotes ' OR 1=1 --"
      ],
      "verification_methods": [
        "behavioral_probe"
      ]
    }
  ]
}